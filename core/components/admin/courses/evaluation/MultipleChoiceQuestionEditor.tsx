"use client";

import { MultipleChoiceQuestionDto } from "@/types/types";

export const MultipleChoiceQuestionEditor = ({question, onChange}: {question: MultipleChoiceQuestionDto, onChange: (question: MultipleChoiceQuestionDto) => void }) => {
    return (
        <div></div>
    )
}