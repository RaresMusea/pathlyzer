"use client";

import { CodeFillQuestionDto } from "@/types/types";

export const CodeFillQuestionEditor = ({question, onChange}: {question: CodeFillQuestionDto, onChange: (question: CodeFillQuestionDto) => void }) => {
    return (
        <div></div>
    )
}