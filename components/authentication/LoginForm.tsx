import { CardWrapper } from "../CardWrapper";

export const LoginForm = () => {
    return (
        //socialOptionsEnabled
        <CardWrapper headerName="Welcome Back! Sign in into your account" backButtonText="Don't you have an account? Register now!" backButtonHref="/register">
        </CardWrapper>

    );
}